{
  "name": "printster",
  "workspaces": [
    "packages/application",
    "packages/infrastructure"
  ],
  "scripts": {
    "bootstrap": "npm install && npm run bootstrap:infrastructure",
    "bootstrap:infrastructure": "cd packages/infrastructure && npm run synth && npm run deploy",
    "start": "npm run start --workspaces --if-present"
  },
  "scriptDescriptions": {
    "bootstrap": "Installs all packages dependencies and then bootstraps packages in the correct order.",
    "bootstrap:infrastructure": "Synthesizes and deploys the default stack in the infrastructure package.",
    "start": "Runs the start command in all workspaces if present." 
  }
}
